# MODULE.md 精简方案

> 创建时间: 2026-05-21
> 状态: 待审核

---

## 现状分析

| 项目 | 数值 |
|------|------|
| 总行数 | 4762行 |
| ##章节（一级） | 65个 |
| ###章节（二级） | 237个 |
| ####章节（三级） | 12个 |
| 总章节 | 314个 |
| Python代码块 | 131个 |
| 表格行 | 327行 |

---

## 精简方案核心

### 合并分组（31个章节 → 6个合并章节）

| 分组 | 原章节数 | 原行数 | 合并后行数 | 精简行数 | 合并后章节名 |
|------|---------|--------|-----------|---------|-------------|
| 增量路径 | 11个 | 1006行 | 约100行 | 906行 | ## 增量路径规范（合并） |
| 布林带 | 3个 | 326行 | 约50行 | 276行 | ## 布林带因子规范（合并） |
| 异常处理 | 4个 | 452行 | 约80行 | 372行 | ## 异常处理规范（合并） |
| 字段规范 | 4个 | 179行 | 约60行 | 119行 | ## 五维度字段规范（合并） |
| 函数设计 | 5个 | 222行 | 约80行 | 142行 | ## 函数设计规范（合并） |
| 代码风格 | 4个 | 295行 | 约100行 | 195行 | ## 代码风格规范（合并） |
| 其他精简 | - | 约550行 | - | 约550行 | 去重代码示例/表格/简化长章节 |
| **合计** | **31个** | **2630行** | **约470行** | **约2160行** | |

---

### 精简后结构预估

| 项目 | 原值 | 精简后 | 变化 |
|------|------|--------|------|
| 总行数 | 4762行 | 约2600行 | -46% |
| ##章节 | 65个 | 约45个 | -31% |
| ###子章节 | 237个 | 约150个 | -37% |
| 代码块 | 131个 | 约80个 | -39% |

---

## 合并详情

### 一、增量路径章节合并（11→1）

**目标章节：**
1. 增量更新返回数据规范（57行）
2. 增量计算 None 处理规范（27行）
3. 增量路径 rolling_ic_mean 规范（91行）
4. 增量路径 period 字段规范（76行）
5. 增量路径返回结构一致性规范（113行）
6. 增量路径因子值有效性检查规范（95行）
7. 增量路径 None 值保留规范（127行）
8. 增量路径向量化计算 IC 规范（129行）
9. 增量路径布林带历史数据必要性规范（91行）
10. 增量路径最小必需历史窗口边界检查规范（106行）
11. 全量/增量 IC 计算等价性规范（30行）

**合并为：** `## 增量路径规范（合并）`

**保留核心：**
- 返回结构（dates/ic_values/rolling_ic_mean等）
- None处理（保留而非隐式删除）
- 向量化计算（避免逐行循环）
- 边界检查（min_periods、rolling窗口）
- 检查清单（合并为一个）

---

### 二、布林带章节合并（3→1）

**目标章节：**
1. 布林带因子必须加载 close 列规范（96行）
2. 布林带因子固定使用 close 列规范（115行）
3. 布林带 %B 计算显式处理 NaN 规范（115行）

**合并为：** `## 布林带因子规范（合并）`

**保留核心：**
- close列加载（required_columns=['close']）
- %B计算NaN处理（显式而非隐式传播）
- 禁止行为（合并为一个代码块）
- 正确实现（合并为一个代码块）

---

### 三、异常处理章节合并（4→1）

**目标章节：**
1. 异常处理规范（69行）
2. 异常处理必须区分严重错误和可恢复错误规范（137行）
3. 异常链保留规范（82行）
4. 异常处理链规范（2026-05-20新增）（164行）

**合并为：** `## 异常处理规范（合并）`

**保留核心：**
- 错误分类（严重vs可恢复）
- 异常链保留（raise from e）
- 禁止行为（合并为一个代码块）
- 检查清单（合并为一个）

---

### 四、字段规范章节合并（4→1）

**目标章节：**
1. ic_metrics 字段规范（54行）
2. factor_direction 字段规范（35行）
3. economic_significance 字段规范（35行）
4. statistical_significance 字段规范（55行）

**合并为：** `## 五维度字段规范（合并）`

**保留核心：**
- 7字段定义（t_stat, p_value, p_value_display, nw_lag, nw_lag_method, is_significant, conclusion）
- 字段映射表格（合并为一个表格）
- 禁止行为（合并为一个）

---

### 五、函数设计章节合并（5→1）

**目标章节：**
1. 函数参数设计规范（37行）
2. 函数返回值契约规范（6行）
3. 函数返回值契约校验规范（92行）
4. 函数签名变更同步规范（21行）
5. period.start/end 语义规范（66行）

**合并为：** `## 函数设计规范（合并）`

**保留核心：**
- 参数传递（避免可变默认参数）
- 返回值契约（必须返回完整结构）
- 签名变更同步（修改签名需检查所有调用点）
- 检查清单（合并为一个）

---

### 六、代码风格章节合并（4→1）

**目标章节：**
1. 变量命名语义清晰原则规范（53行）
2. 字典结构缩进规范（51行）
3. 注释缩进一致性规范（96行）
4. PEP8 import 规范（95行）

**合并为：** `## 代码风格规范（合并）`

**保留核心：**
- 命名规范（避免语义混淆）
- 缩进规范（字典/注释一致）
- PEP8 import（顺序：标准库→第三方→本地）
- 检查清单（合并为一个）

---

### 七、其他精简

1. **去除重复代码示例**（约150行）
   - 每个规范保留1-2个核心示例
   - 去除重复的"正确实现"示例

2. **去除冗余表格**（约100行）
   - 合并相同结构的表格
   - 去除重复的字段定义表格

3. **简化过长章节**（约300行）
   - IC 计算规范（509行→约200行）
   - 保留核心原则，去除冗余说明

---

## 保持独立的章节（不合并）

以下章节保持独立，不参与合并：

| 序号 | 章节名 | 行数 | 原因 |
|------|--------|------|------|
| 1 | 概述 | 11行 | 入口章节，必须独立 |
| 2 | 公共模块同步规范（2026-05-21新增） | 43行 | 刚添加，必须保留 |
| 3 | factor_ic/ 目录规范 | 61行 | 目录结构规范，核心章节 |
| 4 | IC 计算规范 | 509行 | 核心计算逻辑，可简化但不合并 |
| 5 | 增量更新规范 | 94行 | 增量模式入口，核心章节 |
| 6 | 参数传递规范 | 106行 | 参数传递规范，核心章节 |
| 7 | NaN 处理规范 | 90行 | NaN处理规范，核心章节 |
| 8 | 字段去重化规范 | 71行 | 字段去重规范，核心章节 |
| 9 | 输出结构统一性规范 | 包含在第4章 | 输出结构规范，核心章节 |
| 10 | 全量路径与增量路径防御对称规范 | 112行 | 防御对称规范，核心章节 |
| 11 | 可选字段回退逻辑规范 | 148行 | 回退逻辑规范，核心章节 |
| 12 | 统计显著性判断规范 | 14行 | 五维度判断，必须保留 |
| 13 | IC分布一致性判断边界规范 | 20行 | 五维度判断，必须保留 |
| 14 | 引用说明 | 12行 | 结尾章节，必须独立 |

---

## 必须保留的关键内容

以下内容在任何精简操作中都必须保留：

- ✓ 公共模块同步规范（2026-05-21新增）
- ✓ statistical_significance 7字段定义（nw_lag, nw_lag_method）
- ✓ 输出结构统一性规范
- ✓ IC 计算规范（核心）
- ✓ NaN 处理规范
- ✓ 全量/增量路径防御对称规范
- ✓ 可选字段回退逻辑规范
- ✓ 五维度判断规范（全部保留）
- ✓ 所有检查清单（合并而非删除）
- ✓ 所有禁止行为（合并而非删除）

---

## 精简原则

1. **合并相似章节**（去重，保留核心内容）
2. **去除重复代码示例**（每个规范保留1-2个核心示例）
3. **保留所有检查清单**（合并而非删除）
4. **保留所有禁止行为**（合并而非删除）
5. **简化过长章节**（保留核心原则，去除冗余说明）
6. **保持核心章节独立**（不合并14个核心章节）
7. **使用git备份**（每次合并后提交commit）

---

## 风险提示

⚠️ **历史教训**：之前尝试合并布林带/增量路径章节时，导致部分内容丢失（已恢复）

⚠️ **建议分批执行**：每次合并后验证完整性

⚠️ **git备份**：使用git作为安全保障，每次合并后提交commit

⚠️ **关键字段**：statistical_significance 7字段定义必须保留（nw_lag, nw_lag_method）

---

## 执行建议

### 执行顺序（分批执行，降低风险）

**第一批（最小风险）：**
1. 合并布林带章节（3→1，精简276行）
2. 验证内容完整性
3. 提交git commit

**第二批（中等风险）：**
1. 合并异常处理章节（4→1，精简372行）
2. 验证内容完整性
3. 提交git commit

**第三批（中等风险）：**
1. 合并字段规范章节（4→1，精简119行）
2. 验证内容完整性
3. 提交git commit

**第四批（较高风险）：**
1. 合并增量路径章节（11→1，精简906行）
2. 验证内容完整性
3. 提交git commit

**第五批（中等风险）：**
1. 合并函数设计章节（5→1，精简142行）
2. 合并代码风格章节（4→1，精简195行）
3. 验证内容完整性
4. 提交git commit

**第六批（收尾）：**
1. 去除重复代码示例/表格
2. 简化过长章节（IC计算规范）
3. 更新版本号（v1.3→v1.4）
4. 最终验证
5. 提交git commit + push

---

## 验证清单

每次合并后需要验证：

```
□ 公共模块同步规范是否存在？
□ statistical_significance 是否包含7字段（nw_lag, nw_lag_method）？
□ 输出结构统一性规范是否完整？
□ NaN 处理规范是否保留？
□ 五维度判断规范是否完整？
□ 合并章节的核心内容是否保留？
□ 检查清单是否合并而非删除？
□ 禁止行为是否合并而非删除？
```

---

## 版本规划

- 当前版本：v1.3
- 精简后版本：v1.4
- 更新内容：合并31个章节为6个合并章节，精简约2160行

---

## 附录：##章节列表（65个）

| 序号 | 章节名 | 行数 | 处理方式 |
|------|--------|------|----------|
| 1 | 概述 | 11行 | 保持独立 |
| 2 | 公共模块同步规范 | 43行 | 保持独立 |
| 3 | factor_ic/ 目录规范 | 61行 | 保持独立 |
| 4 | IC 计算规范 | 509行 | 保持独立（可简化） |
| 5 | 增量更新规范 | 94行 | 合并到增量路径 |
| 6 | 参数传递规范 | 106行 | 保持独立 |
| 7 | 异常处理规范 | 69行 | 合并到异常处理 |
| 8 | 字段去重化规范 | 71行 | 保持独立 |
| 9 | 日期类型一致性规范 | 33行 | 保持独立 |
| 10 | 输入验证规范 | 21行 | 保持独立 |
| 11 | 公共函数复用规范 | 65行 | 保持独立 |
| 12 | 增量更新返回数据规范 | 57行 | 合并到增量路径 |
| 13 | NaN 处理规范 | 90行 | 保持独立 |
| 14 | 滚动窗口参数规范 | 113行 | 保持独立 |
| 15 | 变量命名语义清晰原则规范 | 53行 | 合并到代码风格 |
| 16 | 数据对齐验证规范 | 133行 | 保持独立 |
| 17 | 极端值裁剪规范 | 84行 | 保持独立 |
| 18 | 主入口错误处理规范 | 75行 | 保持独立 |
| 19 | ic_series 排序规范 | 29行 | 保持独立 |
| 20 | ic_series.index 类型规范 | 53行 | 保持独立 |
| 21 | 函数参数设计规范 | 37行 | 合并到函数设计 |
| 22 | period.start/end 语义规范 | 66行 | 合并到函数设计 |
| 23 | 字典结构缩进规范 | 51行 | 合并到代码风格 |
| 24 | 函数返回值契约规范 | 6行 | 合并到函数设计 |
| 25 | ic_metrics 字段规范 | 54行 | 合并到字段规范 |
| 26 | factor_direction 字段规范 | 35行 | 合并到字段规范 |
| 27 | economic_significance 字段规范 | 35行 | 合并到字段规范 |
| 28 | statistical_significance 字段规范 | 55行 | 合并到字段规范 |
| 29 | 增量计算 None 处理规范 | 27行 | 合并到增量路径 |
| 30 | 全量/增量 IC 计算等价性规范 | 30行 | 合并到增量路径 |
| 31 | 旧缓存兼容性处理规范 | 18行 | 保持独立 |
| 32 | 返回值标记规范 | 21行 | 保持独立 |
| 33 | 错误信息格式规范 | 22行 | 保持独立 |
| 34 | 字典构建规范 | 27行 | 保持独立 |
| 35 | 输出字段口径规范 | 23行 | 保持独立 |
| 36 | 代码维护同步检查规范 | 14行 | 保持独立 |
| 37 | 设计演进清理规范 | 6行 | 保持独立 |
| 38 | 技术指标参数规范 | 91行 | 保持独立 |
| 39 | 浮点数等值比较规范 | 75行 | 保持独立 |
| 40 | 增量路径 rolling_ic_mean 规范 | 91行 | 合并到增量路径 |
| 41 | 增量路径 period 字段规范 | 76行 | 合并到增量路径 |
| 42 | 增量路径返回结构一致性规范 | 113行 | 合并到增量路径 |
| 43 | 函数返回值契约校验规范 | 92行 | 合并到函数设计 |
| 44 | 增量路径因子值有效性检查规范 | 95行 | 合并到增量路径 |
| 45 | 增量路径 None 值保留规范 | 127行 | 合并到增量路径 |
| 46 | 布林带因子必须加载 close 列规范 | 96行 | 合并到布林带 |
| 47 | 布林带因子固定使用 close 列规范 | 115行 | 合并到布林带 |
| 48 | 列表索引访问前必须检查长度规范 | 88行 | 保持独立 |
| 49 | 异常处理必须区分严重错误和可恢复错误规范 | 137行 | 合并到异常处理 |
| 50 | 异常链保留规范 | 82行 | 合并到异常处理 |
| 51 | 异常处理链规范 | 164行 | 合并到异常处理 |
| 52 | 布林带 %B 计算显式处理 NaN 规范 | 115行 | 合并到布林带 |
| 53 | 增量路径向量化计算 IC 规范 | 129行 | 合并到增量路径 |
| 54 | 增量路径布林带历史数据必要性规范 | 91行 | 合并到增量路径 |
| 55 | 增量路径最小必需历史窗口边界检查规范 | 106行 | 合并到增量路径 |
| 56 | 注释缩进一致性规范 | 96行 | 合并到代码风格 |
| 57 | PEP8 import 规范 | 95行 | 合并到代码风格 |
| 58 | 全量路径与增量路径防御对称规范 | 112行 | 保持独立 |
| 59 | 可选字段回退逻辑规范 | 148行 | 保持独立 |
| 60 | 函数签名变更同步规范 | 21行 | 合并到函数设计 |
| 61 | 参数类型约定规范 | 20行 | 保持独立 |
| 62 | 统计显著性判断规范 | 14行 | 保持独立 |
| 63 | IC分布一致性判断边界规范 | 20行 | 保持独立 |
| 64 | 增量模式 period 语义规范 | 25行 | 合并到增量路径 |
| 65 | 引用说明 | 12行 | 保持独立 |

---

## 总结

- **精简行数**：约2160行
- **精简比例**：约46%
- **章节变化**：65个 → 约45个
- **执行方式**：分批执行，降低风险
- **安全保障**：git备份，每次合并后验证

---

请审核以上精简方案，确认后执行精简操作。